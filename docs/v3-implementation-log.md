# v3 整合实施记录

## 基线

- 上游 main：16af879255341ec62bf44ae61c797569600a566d。
- 下载目录与该提交一致，额外包含海晏堂 HTML 原型。
- 本地基线：d837411，标签 baseline/main-20260924；实施分支 feat/game-module-v3。
- 本环境没有 Pollux MCP、CLI 或原机器的 .projectmem；未手工写其生成文件。本记录用于保存本轮问题、决定和验证结果。

## 修复前发现

1. cover 进入旧分页流程，walk 另存一份 run，报告与次日信没有共同完成状态。
2. 照片只有临时路径，换页会丢；照片投稿实际提交固定文字。
3. 支付按钮只写本地付费标志；提交失败被当成待审核。
4. 水力钟三个版本规则不同，新页面在答题前泄露正午答案。
5. 分支音频组件删除 onMute 却仍绑定该事件，揭晓音源可能提前播出。
6. 发布清单误包含 2.25 MiB HTML 原型，主包约 3.23 MiB。现有总包检查使用独立的 4 MiB 项目预算。
7. 原测试中存在已失败的旧页面、旧文案、付费和日期卡断言；新验收须检验真实行为而非保留废弃需求。

## 已批准的决定

八站 v3 主线；不开发支付或独立后端；宿主能力注入；新版独立存档且保留旧数据；历史年份进史料卡，作品日期取首次完成日；正午预测后揭晓；转盘实体确认；可跳过且如实记录；底图加私人记录；公开投稿必须真实服务确认。

## 实施和验证

- `d5717c1`：统一 v3 主线、存档、宿主桥接、原生水钟、照片/个人作品与新录音迁入。
- 清理阶段：从 app.json 注册页、组件与 require 引用链出发，删除支付、旧日期玩法、重复页面/存档、历史 H5、无引用素材及探测工具；明细见 v3-cleanup-manifest.json。
- 音频按真实页状态映射；37个新MP3保留，16个风险条目冻结；保留main的原质量DJ-06。冻结与原始ID别名可能指向同一文件，发布清单按“所有启用ID的文件并集”判断，避免误删序章音频。
- 路线改为人工确认到达的顺序示意；旧坐标只是排布估值，未沿用其精确导航/距离。
- 主包使用自身封面文件，避免首页引用未加载的游戏分包图片；游戏样式不写全局app.wxss。
- 2026-09-25自动检查108项通过，包体字节统计约5.93MiB；真实微信工具已本地编译首页与序章。
- 详细验证及未完成的外部验收见 v3-validation.md。没有推送、上传或发布。

## 次日彩蛋剧情演出

仅 LT1–LT8 引入老师书房场景与逐字对白，正文 UI 保持原样。接力记录与留言沿用现有功能，在对白结束后展示。新增阅读游标、点击补全与推进、已读回顾和生命周期计时清理。详见 v3-letter-scene.md。npm test：113 项通过，0 失败；未完成本轮原生/真机验收。Pollux 当前不可用，使用本文记录。

## Word 原稿剧情补全（2026-09-25）

根据用户明确的【】规则补齐屏幕剧情。正文 165 个有文字段落逐一映射，史料 17 张逐层对照；修复中心亭和终局投影过滤，保留原稿非括号演出句，生成完成后先显示正文再查看作品。原有页面风格和稳定节点 ID 保持不变。详细边界与逐段清单见 v3-script-restoration.md；本轮 Pollux 不可用。

## 史料点击链路修复（2026-09-25）

上轮逐段核对只能证明史料数据完整，未覆盖真实打开卡片时的投影：旧 onOpenCard 在未答题时把海晏堂/大水法截为空、黄花阵截为一层，并隐藏配图。按用户要求移除这些史料限制，保留剧情揭晓门控。17 张卡片、36 层正文、9 个配图引用改为连续显示；层级按钮作为页内跳转，目录和详情使用独立滚动容器，返回目录或重新打开从头显示。复建/原墙及铜版图异体名补齐关键词映射，已遇见名词在后续正文仍可点击。

新增真实 Page 点击处理 + WXML 渲染测试，逐张核对 Word 来源段落与配图；检查未答题、跳过、返回目录、层级锚点及进度不变。npm test 123 项通过，源文件发布预算约 5.96 MiB。微信开发者工具实际打开海晏堂，确认两层全部内容在同一卡片中呈现；工具另有既有 routeDone/webviewId SDK 路由错误，未将其误报为史料脚本错误或宣称全部原生验收通过。无真机验收。Pollux 当前不可用，记录于此。

## 移除剧本规划附录（2026-09-25）

按用户要求删除考察簿“剧本创作说明”入口、附录显示分支以及运行时与测试夹具中的全局设定/总体规划文字。原 Word 未修改，正文剧情与史料卡保持不变。

## 互动玩法独立展示（2026-09-25）

按用户澄清，将 11 项互动的标题、题干、操作说明、观察提示、实体道具说明、控件、帮助、反馈与提交/跳过按钮收进统一 interaction-module。模块沿用现有纸色、绿色和字体，以浅色底、边框、标题与留白区分正文。结果说明放入“互动揭晓”模块；剧情反应和转场仍属于正文。既有翻面/答题/到达条件、存档和原生水钟滚动方式不变。

原稿字段明确拆分为 lines 与 interaction，逐段覆盖夹具同步登记新去向，未通过删减原文实现分类。npm test 125 项通过，包含全部 11 项玩法的模块与按钮归属、原稿文字覆盖、史料点击渲染和原有流程回归。本轮未做原生模拟器/真机视觉验收。Pollux 仍不可用。

## 实景体验审核与微调（2026-09-25）

以 `7ae9d58` 为回退基线，对照 v3 设计稿与用户后续澄清检查故事、实体道具、互动、史料、存档及终局。增加 11 项任务摘要与针对性反馈；已有揭晓的普通谜题可主动看解读，跳过仍据实记录，水钟不开放提前解读。中心亭直接提交可保存文字观察，文字与照片同等有效，回看不能删除记录或改写互动确认。优化站点页眉、真实回看按钮、已署名状态及辅助文字可读性，视觉风格与完整 Word 内容保持。

`npm test` 128 项通过，0 失败；语法/引用/资源哈希/包体预算通过，源文件包体 5.96 MiB。开发者工具实际检查作品、路线与蓄水楼回看排版；真机、实地路线、音频听审未冒充完成。审核对应表与外部验收条件见 v3-experience-review.md。当前工具列表无 Pollux，按规则记录于此。

## 完成后直接进入彩蛋（2026-09-25）

根据用户新要求，署名完成页及作品页增加“直接进入彩蛋”。统一 session.openBonus 仅允许已完成考察的档案进入，玩家主动点击后持久开启来信，支持当天、退出恢复和旧档案回访；已读部分恢复当前彩蛋进度。不修改完成时间，不将主动进入冒充宿主可信时间核验，不影响新考察。未主动进入的自然日来信规则及提醒仍保留。作品页用明确的彩蛋按钮替代“检查来信”主操作，避免等待文案误导。

npm test 130 项通过，包含未完成拒绝、同日进入、持久恢复、旧档案隔离和作品页按钮导航；本轮未做真机验证。Pollux 不可用，记录于此。

## 剧本内容包与可配置结构（2026-09-25）

按用户要求，将现有剧情、页标题、流程衔接、史料关联、道具关联、完整史料、道具说明、任务/提示及既有单选/拼字答案移至 `plate21/module/content/`。旧入口改为编译/兼容适配，不再保存平行正文；历史卡片的兼容 lines 和年份从唯一 layers 自动派生。引擎可注入编译后页表，以便独立验证剧情修订。

增加 `npm run check:story` 并纳入完整测试，检查节点重复/缺失、流程循环/不可达、玩法/史料/道具引用、题目答案、图片和特殊节点；兼容清单阻止直接破坏已保存节点的身份。文档详述普通插页、文案修改、Word 对照及需要专门开发/迁移的边界，不宣称任意新玩法零代码或自动迁移删改节点。

迁移前后所有 43 个页面原有字段逐项一致；原稿及史料覆盖测试继续通过。新增测试实际插入普通阅读页并通过引擎前进，测试坏引用/循环失败、文本修订保留进度及日期。npm test 共 133 项通过，0 失败，发布源文件预算 5.98 MiB；本轮未做真机验证。未改变 UI 外观、存储命名空间、旧存档或自然/主动彩蛋入口。Pollux 工具不可用，记录在此。

## 重新考察界面与沉浸文案（2026-09-25）

用户要求新增真正的重新开始界面，并清理解释/注释文字。复用 walk 容器增加独立全屏“再访”状态，首页、考察簿与作品页均可进入；打开界面和取消不会 reset，只有“重新启程”调用统一 session.restart，保留已完成作品。未新增微信路由，仍可嵌入官方分包。重新开始前保留必要的进度影响说明。

移除首页“八站实景探索/照片与文字记录/演示入口”说明，主入口改为“翻开档案”；清理开发模式、GPS 数据实现、暂无版号、史料完整性宣告、底图构成与重复功能说明。必要操作、失败重试、投稿同意、图注来源及史料全部保留。按用户最新授权，将程序设计参考、给用户展示图样、屏幕暗亮/信件出现/几秒生成等演出说明移出展示，相关历史结论与信件正文保留。原始段落仍在来源夹具；新的 displayText 逐项记录本轮展示修订，详细对照在 v3-immersion-copy-changes.json。此用户要求覆盖此前“所有非【】内容原样上屏”的严格展示规则。

验证：npm test 135 项通过，0 失败；含旧进度取消不变、确认重开保留档案、原稿展示修订/完整史料验证。微信开发者工具实际从首页打开“再访”界面并检查截图，按钮及正文显示正常；没有在用户现有模拟器记录上确认重置，实际重置由隔离行为测试验证。本轮未做真机或录音听审，既有音频补录清单仍需核对。Pollux 不可用，记录在此。

## 大水法鹿犬拖拽与喷水（2026-09-25）

根据用户提供场景图制作空景与透明鹿、十犬素材，加入独立 fountain-puzzle 原生组件，替换 DS1 旧三构件点选示意。两端铜兽保留在背景；鹿犬正确归位后才出现动态水流，支持点选辅助、暂停、错误回退、草稿恢复和只读回看。剧情变更仅限相应操作说明，原文保留于覆盖夹具，并登记展示修订。

npm test 138 项全部通过，资源哈希/引用/发布预算通过；游戏分包 1.58 MiB，总源文件包体 6.64 MiB。已检查开发者工具当前工程，但本轮没有完成新玩法原生模拟器拖拽及 iOS/Android 真机验收，不将自动测试视为真机结果。Pollux 当前不可用，记录于此。

## 剧情与玩法分页（2026-09-25）

按用户要求，将所有含 interaction 的主线节点拆成独立屏幕：默认剧情 → 玩法，结果节点为解读 → 剧情，黄花阵翻面为操作 → 揭晓 → 剧情，中心亭记录完成后另屏阅读剧情。彩蛋接力/留言结束人物对话后不再重复展示正文。整体配色与视觉样式不变。

使用原有 session 草稿保存 screenPart，保持剧情与玩法 ID 稳定；返回优先回到前一屏，不清空作答；分页暂停讲述和 NFC，玩法屏不挂载剧情讲述。未通过的玩法不能进入其后置剧情。跳过隐藏受门控的互动揭晓，不产生空白答案页。

npm test 141 项通过，0 失败；新增逐节点 WXML 互斥渲染、返回保留答案、保存重载和翻面三屏顺序测试，并更新旧混排断言及全主线测试。资源与包体检查通过，总计 6.64 MiB。本轮未完成微信原生模拟器及 iOS/Android 真机分页验收。Pollux 不可用，记录于此。

## 剧情听讲与自动翻页（2026-09-25）

加入首次听读选择、持久化偏好及顶部切换；单个讲述播放器移至导航上方。玩家选择听讲后，剧情录音自动播放，完整结束三秒后继续，下一段接续播放。互动、导航到达、署名、回看、缺音与冻结录音均不会被自动推进。玩家任何触屏/滚动、打开抽屉、静音/重播、后台或离开取消倒计时；后台不自动恢复声音。

npm test 148 项通过，包体总计 6.65 MiB，游戏分包 1.59 MiB。微信开发者工具检查模式弹层与听讲顶部显示，当前无音频的接力记录页保持停留；工具出现热重载 routeDone 系统报错，未宣称全链路音频原生验收。真实录音逐字听审及 iOS/Android 验证仍待完成。原停用音频保持停用，具体原因与补录清单继续保留。Pollux 不可用，记录于此。

## 彩蛋语义段落（2026-09-25）

按用户反馈，移除逐句/85 字分页，将 LT1—LT8 的 58 个旧片段按语义合并为 18 段，逐字演出和一键补全文字保留。原始正文不修改，分组在 story.dialogueGroups 中配置并校验。扩大对白阅读区，正文滚动区域不触发下一段，明确使用“下一段”按钮。动态状态段落完整保留，旧存档句级游标映射至对应语义段落，新存档记录段落版本。

npm test 150 项通过，包含完整原文无丢失、分组数量、旧/新游标恢复、逐字补全和生命周期回归。包体检查通过，总计 6.65 MiB。本轮未完成真机长段落排版验收；Pollux 不可用，记录于此。

## 最新 Word 同步（2026-09-25）

按 第廿一图v3_副本2(1).docx 同步 22 个既有节点，新增方外观人物、用途及揭晓三节点（FQ1/FQ2/FR1）。史料卡正文不变；制作备注不进入玩家正文，明显标点/错字做最小修正。原有大水法、分页、听读模式与彩蛋段落保留。改稿节点旧录音暂停调用，补录清单明确保留，未声称新稿已配音。

npm test 153 项通过，新增判题/跳过/门控、旧存档回看兼容、改稿音源禁播及原文覆盖检查；包体预算通过。具体差异和验收范围见 v3-latest-script-sync.md。本次尚未验收新增题的原生模拟器/真机，Pollux 不可用。

## 工作区清理（2026-09-25）

按用户要求清理 D:/kc/ymy 工作区无用旧文件，保留 main 与 feat/game-module-v3 已入库内容。删除 49 项未入库文件共约 522 MiB（他项目“拾迹创游”材料、内网隧道脚本与日志、一次性调试脚本、旧演示 showcase*、test 旧截图与生成原图/台架输出、test/node_modules、bgm 与 audio 生成源、docs/plates-20 来源图与源 PDF、docs/sl-source-images 工作稿），另将 7 张运行时未引用的生成原图与 4 个无同哈希副本的录音旧草稿移出分包至 docs/compliance/sources、docs/compliance/sources-audio。逐项去向见 docs/v3-cleanup-20260925.json。来源与许可证文字记录、史料来源 URL（docs/plates-20/manifest.json）、可用重取工具（tools/fetch-plates-20.py）与人工验收结论（docs/v3-validation.md）保留；未入库文件删除后不能由 Git 找回。

同时把 .projectmem、.grok、.kimi-code、.playwright-cli、.env、CLAUDE.md、docs/compliance/sources* 补进 project.config.json 打包排除清单，并重建 docs/v3-resource-manifest.json。npm test 153 项全部通过，资源哈希与包体预算通过：主包 0.71 MiB、游戏分包 1.60 MiB、总计 6.67 MiB，均在主包/每分包 2 MiB、总计 20 MiB 预算内。清理前该检查因本地生成源与工具残留计入主包而失败（主包 99.34 MiB、总计 115.21 MiB）。

本轮为文件清理，未改动玩法、剧情与录音；16 个冻结录音的补录清单、37 个新版录音的逐条听审、iOS/Android 真机与微信开发者工具人工验收均未在本轮进行。Pollux 不可用（本环境无该工具接口），按约定记录于此。

## 残留工作区清理（2026-09-25）

按用户要求清理 D:\kc 下与本项目相关的残留文件夹，只保留 D:\kc\ymy（feat/game-module-v3）与 main 的工作能力。已删除 12 个残留文件夹共约 1.56 GB：ymy-cdce962、ymy-upload（本仓库 worktree，已 git worktree remove）、ymy2、ymy2-wt-nfc、plate21-clean、plate21-player-fix、plate21-ui、plate21-ui-pass、yuanmingyuan、yuanmingyuan-zodiac-miniprogram、ymy-web、ymy-audio-backup-20260912。删除前先把不能从 GitHub 找回的内容备份至 D:\kc\_archive-20260925（约 258 MB，含 git bundle、未提交改动 patch、未跟踪文件与无 git 目录的压缩包，说明见该目录 README.md）：未推送分支 fix/player-pass、backup-main-1.23、feat/xieqiqu-nfc、fix/word-plays、feature/water-demo-p0 均有 bundle 留存。

未改动的两处：D:\kc\ymy-check-5ac1255 是本次清理期间出现的活动 worktree（分支 fix/report-exit-navigation，今日 15:14 创建、16:10 仍有修改），属他人/其他会话正在进行的工作，按规则不删除；GitHub 远程的 feat/banhen-ui、feat/plate21-cultural-experience-v4、feat/ui-fixes-and-ticket 三条旧分支未动（共享远程，需用户明确指示）。本仓库本地分支 main、feat/game-module-v3 保持不变。Pollux 不可用，按约定记录于此。
## 清理 E2 剧本批注泄露（2026-09-25）

按用户要求复查最新分支各页面状态与按钮跳转逻辑：先用仓库 harness 生成 78 份状态快照做文本层体检，再用微信开发者工具（CLI + miniprogram-automator，模拟器，AppID wxa284707129230b80）真实点击复核。发现 E2"地图辨位"互动正文向玩家显示批注式文本"答案：d"（content/story.js 的 interaction.lines，随 5ac1255 剧本同步带入）。该模块本是 quiz-direction（辨认方向题）的揭晓屏（requires 门控，解题后才出现），但正文把选项编号当作文案。本次按答案 ID（ne）对应的选项标签，把揭晓文案规范化为"答案：东北"；原文"答案：d"保留在 test/fixtures/v3-script-coverage.json 的 source/text 字段，并以 displayText 记录本次规范化映射。feishu-import 原始提取不动，不擅自扩写解读。

npm run check:story 通过（46 节点，流程、史料、道具与答案引用完整）。微信开发者工具内为模拟器验证，iOS/Android 真机另行安排。Pollux 当前不可用，记录于此。

## 修复考察档案出口的页面栈堆积（2026-09-25）

同一轮复检中，用微信开发者工具模拟器实测按钮跳转：入口→考察→考察档案→各出口的完整链路。发现 report 的"回到考察""直接进入彩蛋""重新考察"三个出口都用 redirectTo 重建 walk，页面栈变成 [index, walk, walk]（两个 walk 共享同一 session，画面一致，用户无感知）。后果实测复现：返回首页要按 3 次系统返回；"合上档案"第一次只退掉重复页、画面无变化，像按钮失灵。改为来路页就是考察页时用 navigateBack 返回原实例（onShow 自动恢复；"重新考察"额外触发原实例的重启确认屏），没有可返回来路或返回失败时仍按原 URL 重定向兜底，直达档案场景不受影响。新增两条单测覆盖"复用原实例"与"无来路/返回失败兜底"，既有 redirect 断言保持通过。

npm test 155 项全通过（含新增 2 条），脚本/JSON/引用/WXML/资源哈希/发布预算检查通过，包体总计 6.67 MiB。跳转复验在微信开发者工具模拟器完成，iOS/Android 真机另行安排。Pollux 当前不可用，记录于此。

## 信封谜题答案术语门控（2026-09-25）

按用户要求：信封谜题（quiz-envelope，X3 揭晓答案为“黄花阵”）解开前，所有显示文本中的“黄花阵”三字隐藏为“？？？”，解出（solved/assisted）或诚实跳过者到达黄花阵导航站（M2）后恢复原文。新增 flow/term-gate.js 统一门控，在四个显示出口替换：flow/screen.js 屏幕模型、progress/build.js 进度与站点名、utils/report-renderer.js 考察档案（含导出图）、pages/walk/walk.js 史料词条目录与词条卡。剧本文本本身在 X3 前没有该词，实际泄露面是站点名、史料词条、道具提示与地图站点片；X3 揭晓块维持原门控，跳过者不揭答案，仅在到达站点后恢复普通文本显示。

npm test 159 项全通过（新增 4 条术语门控测试；v3-ui 词条渲染测试的场景前提更新为信封已解，遮蔽行为由新测试覆盖）。包体总计 6.67 MiB。微信开发者工具内尚未复核渲染效果，iOS/Android 真机另行安排。Pollux 当前不可用，记录于此。

## 输入框提示文字对齐修复（2026-09-25）

按用户反馈“部分输入框的提示文字在点击时会移动”：排查 FN4 署名、X2 信封拼接、H4 观察笔记、LT7 接力与报告补充记录共 5 个输入控件。根因是 placeholder 与输入文字的行高不一致：walk 的 input 固定高度但无 line-height，textarea 的 1.7 行高只作用于输入文字，报告补充记录另有 auto-height 高度重算。为全部控件补 placeholder-class（.ph-line/.ph-block/.ph-note），字号与行高同正文对齐，input 另固定 line-height 48rpx 与内容区一致。

npm test 159 项全通过。模拟器静态渲染正常；placeholder 点击抖动属真机像素行为，需 iOS/Android 真机确认修复效果。按用户规则本次改动只做本地提交，未推送 GitHub。Pollux 当前不可用，记录于此。

## 入口封面与谜题素材图的裁切修正（2026-09-25）

按用户反馈修正图片呈现：入口封面（420×560 竖图）此前以 aspectFill 装进 340rpx 横条，画面被裁掉上下各约三成且主体位置不对；现改为外框裁剪加图片下移（cover-frame 427rpx + margin-top -429rpx），在接近原横条的高度内显示画面下半部（50%）的建筑主体。H5 图案选项（320×320）与 H4 观察点（320×290）素材以 aspectFill 裁进 190rpx 横框会切掉图案上缘，改为 aspectFit 完整显示并将框高调至 240rpx。另经 DOM 几何测量，入口与考察页的按钮、图片、容器左右边距均为等距（28/33px），按钮“不在中间”疑为封面比例失衡的连带观感，待真机复核。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 史料卡按钮布局修正（2026-09-25）

按用户反馈修正史料卡（history-card）内按钮形状与文字布局：“‹ 返回历史目录”此前是满宽 360px 的细条（高 35px）且文字居中，改为靠左的内联小按钮（121×40，带描边与圆角）；层级按钮（概览/深入/延伸）此前字号 24rpx 配 88rpx 高框、比例失衡，改为 26rpx 配 76rpx、上下左右居中并统一 6rpx 圆角，与全局按钮风格一致。经 DOM 几何实测确认。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 移除口吻化提示表述（2026-09-25）

按用户要求删除“这一页，请读一读”式的旁白提示及同类口吻表述：听讲模式提示行整行移除（含“轮到你来探索了”“到达后，再继续”两个变体）；来信场景按钮“轻触补全文字”改为“补全文字”、空态“轻触对白，即可开始阅读。”改为“点击开始阅读。”；模式选择说明改为中性表述（“听完停留三秒自动继续，互动页暂停。”“手动翻页，可随时播放录音。”）。功能性说明（照片/文字记录规则、按钮功能名）保留。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 导览模块文案与图标整理（2026-09-25）

按用户要求整理地图导览模块（任务 2/3/4/5）：(2) 史料卡底部按钮“读完，返回剧情”改为“返回”；(4) 导览路线列表删除“还没到”状态小字（状态为空时不再渲染该行），保留“进行中”并确认其显示位置在当前站标题下方；(5) 检查地图模块：路线 8 站顺序与序号、术语门控（未解信封时站名显示“？？？”）、定位成功显示地图与失败降级文案、导引浮钮拖动边界与点击开导览、nav-model 不猜测坐标均正常；(3) 导引浮钮的字符图标“⌖”更换为 Lucide Icons v1.48.0 的 compass（ISC 许可、可商用），以 base64 内联于 .nav-symbol 样式，来源与许可证全文登记于 assets/NOTICE.md 与 assets/licenses/lucide-icons-ISC.txt。

npm test 159 项全通过（含资源与许可检查）。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 引导文案与按钮样式修正（2026-09-25）

按用户要求修正引导文案与按钮样式（任务 7/8/9/13/14）：(7) 删除 E1 的说教式引导“先选一个方位，再确认答案。”；(8) 地图道具归还说明“辨认方向后，回到页面选择你的答案。”改为纯指令“用完后放回档案袋。”；(9) 回看面板删“重读这一页”，只保留“回到当前进度”，去箭头，并改为居中按钮（原为满宽浅色块看不出是按钮）；(13) “查看我的实际位置”等 secondary 按钮文字居中（choice 选项保持左对齐）；(14) “开始探索 →”“继续 →”“直接进入彩蛋 →”文案中的箭头移除。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 谜题页重复表述清理（2026-09-26）

按用户要求将 H3 的去重推广到所有谜题页：道具卡 returnWhen 统一改为纯道具处置动作（music/maze/dial/prints/patterns/envelope/bulletin 七条，删除“点某按钮”“回到页面…”式指路）；模块正文删除与道具卡/任务卡重复的句子（X2 与 propPrompt 同句、DS1 操作指令尾句、H3 冗余小标题“翻面揭晓答案”）；任务卡说明去按钮指路（X1、H3）；谜题页内与任务卡重复的静态提示删除（prop-flip 提示句、H4 照片说明句）。剧本原文经 test/fixtures/v3-script-coverage.json 的 displayText 记录移除与精简映射（173、186 置空豁免，259 同步为精简句），原文保留在 fixture 与 feishu-import 提取中。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 单行输入框跳动与光标偏移修正（2026-09-26）

按用户反馈（X2“输入地点名称”、FN4“写下你的名字，也可以留白”两个输入框点击时文字跳动、光标位置不对）：根因是 input 控件高度 92rpx 与行高 48rpx 不一致，文字按行高排、光标按控件高算导致错位。改为单行输入框标准解法——行高等于控件高度（92rpx）、垂直 padding 归零、placeholder 行高同步 92rpx，使文字、提示与光标落在同一行盒，聚焦不再跳动。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 跳转穷举校验（2026-09-26）

应用户要求确认“其他页面不会出问题”：新增 test/v3-navigation.test.js 穷举 46 节点 × 3 种存档场景（正常解题 / 全部跳过 / 跳过整站），断言回看前进的目标永远“可进入 + 在 next 主链上 + 不原地打转”或停在链尾（按钮退化为返回当前进度）；全部 skipTo 目标在主链上、不越界；任意节点沿 next 主链均可达 LT8 无绕路。npm test 162 项全通过。该校验长期生效，后续剧本改动会自动覆盖。

npm test 162 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## UI 批量修正一：小改与排版（2026-09-26，锚点 checkpoint/ui-batch-start）

按用户确认继续处理剩余 UI 问题（22 不改、25 挂起）。第一批（20/29/34/35/32）：标题 letter-spacing 尾隙负 margin 抵消（“第廿一图/第二十一图”视觉居中）；来信“下一段”按钮文字区给独立箭头留位（“▾”不再叠字）；“另写一份”改“重写”；关联地点 picker 文字与箭头拆行、箭头换实心三角固定行尾；“一句话/一张照片/一个心愿”三按钮等宽排列。第二批排版（10/12/14/19/11/18）：站名节标题（序章/西洋楼入口）由灰弱 caption 改为清晰节标签；实地导引“当前考察进度”加左标线与标签/站名层次；导航站站点片改 4×2 等宽网格；问题卡标签与问题标题合并一行（“回答 · 辨认方向”）；本页史料按钮从页面底部移到标题下方成一行入口。

npm test 162 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## UI 批量修正二：水钟静态图与旧画重建层（2026-09-26）

第三批（21/24）：(21) 海晏堂水钟三张静态场景图（paint/wide/close）比例各异（960×561/860/643），aspectFill 裁剪量不同导致“看下一幅”切换时画面跳动、图片大小不一——统一中心裁剪为 960×640，切换窗口一致；(24) DS2“旧画与眼前”重建层的代码绘制构件（几何柱子/拱门与文字占位“鹿/猎狗”，生成图观感）改为真实剪影素材（dashuifa-deer/dogs.png，与拖拽件同源），水柱线条保留。资源清单已刷新。(30) 语音段数低于页面为资源现状（37 条录音未覆盖全部段落、26 条冻结待补录），播放键时有时无是无录音段不显示的设计；补录到位后自然对齐，暂不改逻辑。

npm test 162 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 谜题页结构重构：道具卡 / 问题卡两段式（2026-09-26）

按用户确认的方案重构实体道具与解谜页结构（此前删句子未解决结构杂乱）：原一屏含三层标题（页面标题/任务卡标题/模块标题）与两套指令（任务说明/道具指令/模块提示）、三种混杂边框。现统一为“实体道具卡 → 问题卡”两段流线：道具卡为唯一指令来源（卡标签用道具动作短语，步骤编号 ①②，归还提示为尾注小字）；问题卡含小标签“回答”、问题标题、任务提示小字、谜面与全部控件；主按钮与跳过留在模块尾部。任务卡层删除（任务即页面标题，task.instruction 降级为问题卡内提示小字），walk-content 的道具卡同步同款结构与样式，边框统一为一种卡框。

实现面：walk-interaction.wxml / walk-content.wxml 模板重构、walk.wxss 新增 .puzzle-card/.card-label/.question-title/.question-hint/.prop-step/.prop-note 统一样式、v3-ui 结构锚点断言同步（question-card）。13 个谜题页全部套用，剧情/导航/来信页不变。npm test 162 项全通过；模拟器 DOM 实测两卡布局正常。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 修复拖放槽位被页面样式压掉定位（2026-09-26）

用户截图确认大水法拖动的“池中/环绕中央”槽位偏移（即此前反馈的“拖动改毁了”）：根因是为按钮箭头加的 .p21-page button{position:relative} 穿透进 fountain-puzzle 组件，且特异性（类+标签）压过组件的 .drop-zone{position:absolute}，使两个拖放槽掉回文档流、堆在场景图下方。修复：移除全局 button 的 position:relative（入口页按钮规则保留，因其无组件），箭头锚点改为仅 .primary 等具体按钮类持有；并把组件槽位选择器加固为 .fountain-game .drop-zone，防止再次被页面样式覆盖。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 回看跳转修复与跳转流程图（2026-09-26）

按用户反馈“从黄花阵后面直接跳到最后”：定位为 engine.reviewNext 在遇到打不开的节点（被跳过谜题的揭晓节点，如 H2 是 H1 的 revealOf；或未解锁站内页）时提前返回空，回看主按钮落到 session.resume（断点=最后完成处）。修复为跳过打不开的节点继续向后找下一个可回看页，仅真正链尾才“返回当前进度”；v3-screen 回看断言同步（跳过谜题页回看也提供“继续回看”）。全部按钮跳转重新核对：46 节点 next/skipTo 链无断链，report 三出口 navigateBack，页内回看回退不换页，门控（LT/揭晓/解锁）正常。新增 docs/v3-navigation-flow.md：页面栈、节点主链与跳过线、按钮行为映射、onPrimary 决策图、门控规则与本次修复（mermaid 流程图）。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 水钟按钮、报告页与彩蛋状态修正（2026-09-26）

按用户要求处理三项：(22) 海晏堂水钟控件按钮等宽均分排列；正午喷水经权威史料核查（多源一致：十二生肖各时辰依次喷水、正午同时涌射），游戏揭晓与 14 时羊首判定均与史实一致；预测环节不判对错为观察实验式设计，暂维持；水声功能存在（默认关，按钮开）。(28) 报告页作品改为记录装箱（多条笔记同页、照片每页至多 4 张），不再每条记录各占一个画框；画内遗址示意线稿（“AI 配图”观感）改为素留白框；无照片时照片区整块隐藏（页内与画内），保留“补充一条记录”编辑入口；页内与画内重复的“八站记录”区删除（保留画内）。(36) 彩蛋已读后报告页状态更新：letterRead 显示“彩蛋已经读完，可以随时重读。”与“重读彩蛋”按钮。(23) 大水法拖动经 git 对比确认组件与坐标逻辑零改动，错乱现象待用户提供截图定位。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 按钮居中与箭头独立修正（2026-09-25）

按用户要求重做两项：(13) “查看我的实际位置”按钮整体居中（自适应宽度，不再铺开满宽）；(14) 按钮箭头不参与文字居中计算——footer 主按钮（开始探索/继续等）、重启屏“重新启程”、来信场景前进按钮的箭头均改为独立绝对定位（右侧、垂直居中），文字单独居中；同时为按钮补 position:relative 锚点。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。

## 入口按钮箭头独立定位（2026-09-25）

按用户要求修正“翻开档案 →”按钮文字不居中的问题：箭头此前作为文字一部分参与居中计算，把“翻开档案”挤偏。改为箭头独立绝对定位（右侧 30rpx、垂直居中、脱离文本流），文字“翻开档案”单独居中于按钮，箭头保留动作暗示。

npm test 159 项全通过。本地提交，未推送 GitHub，微信平台未更新。Pollux 当前不可用，记录于此。
