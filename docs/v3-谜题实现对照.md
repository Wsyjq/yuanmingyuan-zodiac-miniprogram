# v3 谜题 × 现有小程序对照

> 正文：飞书《第廿一图》v3 `Wxj1drAMDoIoaqxjubwcQhUPnkh` 修订 **3122**（2026-09-21 拉取）。
> 代码：worktree `D:\kc\ymy-wt-v3-puzzles` @ `78638df`（`feat/v3-puzzle-audit`）。
> 判定：游客主路径听不到「谜题」二字——下表编号只给制作。

三种结论：

| 结论 | 含义 |
|---|---|
| **已做，可留** | 交互和判定已经对得上 v3，最多改文案 |
| **已做，要改** | 页面在，题型或答案和 v3 不一致 |
| **能做，未做** | 技术上没有硬障碍，需要新页或新拍 |
| **能做，有坑** | 能做，但有平台/素材/史料坑，不能当过关硬门 |
| **不做** | 正文已划掉，或推动者否决 |

---

## 总表

| 站 | v3 题 | 现页 | 结论 | 改动量 |
|---|---|---|---|---|
| 序章2 | 长春园方位四选一 | 无 | **不做**（正文划掉；荏苒：没必要） | 0 |
| 入口 | 半字拼「黄花阵」 | `s1-decode` | **已做，可留** | 文案 |
| 谐奇趣 | 声景侦探 六选多 | `waypoint?site=xieqiqu` | **能做，未做**（页在，玩法是左右同响+取景框，不裁判） | 新拍+音频 |
| 蓄水楼 | DJ-07 物理题 | `waypoint?site=xushuilou` | **能做，未做**（现为比高+五段拼卡，不判分） | 新拍 |
| 黄花阵 | ①作用 ABCD | `s2-quiz` | **已做，要改**（现在是开放作答） | 中 |
| 黄花阵 | ②名字 语音/文字+API | `s2-reveal` | **已做，要改**（文字+本地词；语音/API 未接） | 中 |
| 黄花阵 | ③亭子中西 四槽拍照 | `s2-blend` | **已做，可留** | 小 |
| 黄花阵 | ④万字纹四选一 | `s2-pattern` | **已做，可留** | 小 |
| 方外观 | ⑤三文化 六选三 | `waypoint?site=fangwaiguan` | **能做，未做**（现页写明「都不是题」） | 新拍 |
| 海晏堂 | ⑤水力钟正午（重号） | `s3-comic` | **已做，要改**（现为两问：马 / 午时） | 中 |
| 海晏堂 | v2 转盘七尊 | `s3-zodiac` | v3 表已无此题 | 考虑下线 |
| 海晏堂 | v2 水显纸马首 | `s3-water` | v3 表已无此题 | 考虑下线 |
| 大水法 | ⑥猎狗逐鹿 AR | `dashuifa` | **能做，有坑**（现为 120s 静默+听什么） | 新拍 |
| 大水法 | ⑦北侧是远瀛观 | `dashuifa` | **能做，未做** | 小 |
| 雨果 | 时间轴五事 | `s4-timeline` | **已做，可留**（v3 未标号，正文仍有时间卡） | 小 |
| 雨果 | ⑨四件遗物放到哪 | `s4-timeline` 文末静卡 | **已做，要改**（现在只展示，不放置） | 中 |
| 终局 | 拼第二十一图 | `finale` | **已做，可留** | 残片名单 |
| 终局 | 八位密码 | `s4-password` | **已做，可留** | 0 |
| 报告 | 明信片 | `report` | **已做，可留** | 0 |

v3 编号本身有洞：两个「谜题5」，没有「谜题8」。对照时不要按号排序，按站走。

---

## 逐题

### 1. 半字 → 黄花阵　已做，可留

v3：信封封口下半 + 信背上半 =「黄花阵」。卡住两条提示。

现页 `s1-decode`：sealed → reading → 输入「黄花阵」，提示两级，可恢复。对得上。

只改文案是否跟 v3 信封口径（印刷截开，不是撕）。

### 2. 谐奇趣·声景侦探　能做，未做

v3：30 秒分层声景 → 六选多（A 小拉琴 B 西洋箫 C 琵琶 D 笙 E 班竹板 F 水法；对 A/B/C/F）。全对过；对 3 且错 ≤1 也过；否则重听。通关句挂【SL06】。NFC 只是入口，不能当过关。

现页：顺路散页，**不设谜题、不裁判**。玩的是左右同响 + 取景框卡。`bgm-07x` 中乐含笙，不能当本题素材。

技术：

- 多选判定、重听、通关剪影：现成（`InnerAudioContext` + `completePuzzle`）。
- 音频：分轨混音 v1 已在本机 `bgm/xieqiqu-soundscape/dj06-xieqiqu-soundscape-30s.mp3`（不入库），待盲听。
- NFC：贴卡打开页面可以（苹果要点通知）；页内读 NTAG 苹果不行。主入口用页上「听」+ 小程序码。

要改：谐奇趣从「不裁判散页」改成有对错的一拍；选项里的「水法」不能做成可点史料词。

### 3. 蓄水楼物理题（DJ-07）　能做，未做

v3：信封里特刊。图意是蓄水池位置越高、喷得越高。正文只有「信封里面似乎有线索【DJ07-物理题】」，选项 callout 还没写全。

现页：三声「水怎么上去」+ 仰头比高 + 五段咬合卡，**全部不判分**。

技术：一张示意图 + 三/四选一，完全能做。注意两座蓄水楼不要混（谐奇趣西北 ≠ 海晏堂锡海）。题前只挂「蓄水楼」词，原理挂题后【SL04】。

### 4. 黄花阵① 作用　已做，要改

v3：ABCD，C = 中秋灯会。

现页 `s2-quiz`：开放输入，命中「中秋／灯／宫女／玩／赏」即过，可跳。

改成四选一即可，判定更简单。史料卡文案已经是灯会。

### 5. 黄花阵② 名字由来　已做，要改

v3：语音或文字，**调用 API 检验**。

现页 `s2-reveal`：文字 + 本地 `classifyHuanghuaName`。注释写明语音/API 是 TODO。

主仓工作区另有未提交的 `answer-judge.js`（本地分类器 + 可选远程，3 次后降级四选一）——**不在本 worktree**。

技术：文字路径已经能玩通。语音要 `wx.startRecord` / 同声转写，外网 API 要合法域名；A1 零服务器下远程判题要宿主提供 endpoint，否则保持本地。v3「调用 API」不是硬门槛。

### 6. 黄花阵③ 中西四图　已做，可留

v3：四处细节拍照，合成考察卡。

现页 `s2-blend`：四槽真实拍摄，不伪造识别，进报告。四处细目与 v3 四条一致（穹顶飞檐 / 檐角立兽 / 莲座宝瓶 / 双天鹅蝙蝠）。

### 7. 黄花阵④ 万字纹　已做，可留

v3：四种图样选出墙上的万字纹。

现页 `s2-pattern`：四图（万字 / 贝壳 / 卷草 / 花篮），点万字过。旧稿「答案是水」已不在判定里。

### 8. 方外观三文化　能做，未做

v3：六张元素卡选三项。对 A 两层楼体、B 中式重檐顶、C 阿拉伯文碑。

现页：纹样划记 + 等候卡，**明确「都不是题」**。到站正文还写出「两层／中式顶／阿拉伯碑」，会泄这一题。

技术：多选与声景同一套。先改到站文案（卡和题前都不写三项），再上题。

### 9. 海晏堂水力钟　已做，要改

v3：一题。正午什么样？正确答案「十二生肖同时喷水」。

现页 `s3-comic`：两问（第三格＝马，齐喷＝午时），漫画推理。答案等价，题型更重。

改成 v3 四选一即可；漫画可留作题面图，不必两问。

### 10. v2 转盘 / 水显　v3 已不收

`s3-zodiac`、`s3-water` 仍在生产路由。v3 DJ 表已无花纹转盘、水显纸。海晏堂收成选择题（+ 大水法 AR）。

这两页不是「做不了」，是 **v3 可能要下线**。下线前要改日期卡张数（现在这两题各发一位月/日数字）。

### 11. 猎狗逐鹿 AR　能做，有坑

v3：AR 把铜版轮廓叠到遗址，缺三组：梅花鹿→池心，十猎狗→环绕，卷尾铜兽→东西两端。归位后恢复喷水。

现页 `dashuifa`：120 秒静默 +「听见什么」三选一，**无谜题**。工程里明确「不做实时 AR」。

真 AR（`wx.createVKSession`）户外废墟无标记、光照乱，不能当过关硬门。

能落地的替代（同一题意）：

1. 相机实时画面上叠半透明铜版线稿，三件拖到热区（2D，不求平面跟踪）。
2. 更稳：用铜版图当底，三件拖回空位，不调用相机。

素材要可商用的鹿/狗/铜兽剪影。通关前不要在史料卡里写摆法。

### 12. 远瀛观　能做，未做

v3：大水法北侧高台上是什么？答案远瀛观。

一题选择或短输入，挂在大水法页后即可。远瀛观不做史料卡。

### 13. 时间轴　已做，可留

`s4-timeline`：1747 / 1760 / 1860 / 1861 / 2010，拖或点。v3 雨果章仍「时间轴下方生成时间卡」，只是没标谜题号。

### 14. 四件遗物放到哪　已做，要改

v3：四件拖到四处（石屏风回园 / 石鱼在北大 / 七柱回中国 / 鹿狗不在原位）。

现页：时间轴通过后 **四张静卡展示**，不放置、不校验。文案四条已经对。

改成四槽拖放（或点卡点地点），复用时间轴那套 `drag`。

### 15. 拼图 / 密码 / 明信片　已做，可留

`finale`、`s4-password`、`report` 明信片都在。结局残片名单仍写养雀笼、线法画，和 v3 站序不一致，要另改名单，不是玩法引擎问题。

---

## 建议开工顺序（本 worktree）

1. **改题型、页已在**：黄花阵①改 ABCD；海晏堂改正午一题；遗物改放置。
2. **新拍、引擎现成**：声景多选（音频用已出的 30s）；方外观六选三；蓄水楼物理三选一；远瀛观一题。
3. **有坑、单独拍板**：猎狗逐鹿用 2D 叠合还是真 AR；转盘/水显是否下线（动日期卡位数）。
4. **明确不做**：序章2 地理题。

NFC 不进第 1、2 步。
