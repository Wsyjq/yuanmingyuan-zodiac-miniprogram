# 验证记录

验证日期：2026-09-10。基线 `main@6c4c5c3`；以下是桌面测试结果，不是正式云部署或微信真机验收。

| 检查 | 结果 |
|---|---|
| `npm test` | 138 / 138 通过；含最新 main 的 110 项与本轮 28 项 |
| 语法、JSON、页面与资源引用 | 通过；26 条生产路由 |
| 第三方资源结构/许可证登记 | 通过；修正原仓库 ANI-01 过期校验值 |
| 包体预算 | 主包约 0.23 MiB，分包约 1.44 MiB，总计约 1.67 MiB；音乐、云函数、资料全文与测试均在包外 |
| 页面预览 | 48 个状态；320×568、375×812、430×932 三种屏幕宽度，无横向溢出或页面脚本错误 |
| BGM 开发试听 | 14 个注册文件均可提供 206 Range 分段响应；原文件不变 |
| Git 差异空白检查 | 通过 |

新增覆盖：所有节点连续跳过到终章且不发答题奖励；稍后与回看；实际位置恢复；v2 原始备份与旧报告；无照片草稿与陈旧编辑拒绝；本机保存失败补发；资料偏好；北京时间跨日/月/年；未发布附页深链隐藏；原页面 slot 绑定；长文字导出换行；音频跨组件、断点、拖动、配乐压低、后台、中断、静默占用、重试、连续播放；云身份隔离、媒体归属、幂等、断网、回执丢失及再次冲突。

## 原仓库校验问题

基线 `npm test` 在 ANI-01 校验处失败，尚未开始功能单元测试。将本地 Anime.js 4.5.0 与 npm 官方发布包的对应文件逐字节比较，一致；官方元数据 integrity 与锁定记录一致。因此只更新 third-party-lock 中该文件 SHA-256 为 `8d5b3a58a1f64023a04a4cedeef135a2263b18da04b35177ac13438a0bad033b`，不改库文件。该库仍不随小程序包发布。

## 页面截图的解释

截图由仓库页面渲染台架生成；原页面/组件的 JS 与 WXML 驱动数据，再由 Chrome 排版。补齐了原生 slot 的父级作用域，以及 textarea、slider、switch、video 的预览映射。截图用于检查排版与可见状态，输入文案是测试样例，不是游客真实记录。

本机未安装对应版本的 Playwright Chromium，使用已安装的 Chrome 通过 `PLAYWRIGHT_CHANNEL=chrome` 运行。只处理测试站点的 favicon 204；其他资源 404 仍记为错误。

[关键页面截图](screenshots/README.md)

## 仍需真机 / 测试云环境

- 微信开发者工具编译、iOS / Android 上后台主讲解、锁屏控制、耳机拔出、来电及不同基础库的混音行为。
- 真机图片选择、压缩、持久化、相册授权拒绝后重试，长文字/多照片的纪念图实际保存。
- 部署测试云后，用两账号/两设备验证身份、权限、媒体读取、实际网络中断与容量限制。
- 正式媒体托管地址与微信合法域名尚未配置；当前没有正式旁白，次日故事也未发布。
- 现场路线开放、人流、90—120 分钟节奏与实景注意力比例，仍需园区走测。本轮未拍摄、录音、印制或发布。

工程记忆工具 Pollux 在当前会话不可调用，未伪造调用或自动记录；工作范围与结果保存在根目录 plan.md、PROJECT_MAP.md、此交接包及提交中。
