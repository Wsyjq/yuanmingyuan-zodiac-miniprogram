# v3 验证记录

核验日期：2026-09-24—25。本轮所有验证使用本地代码，没有上传或发布小程序。

## 自动验证

`npm test`：108 项测试通过、0 失败（最终提交前会再次核对）。覆盖：

- 八站正常作答与整条跳过路线、错误多次不自动通过、实体转盘、文字替代照片的协助状态。
- 当前页与恢复点分离，未开放节点与答案页深链拦截；回看不改变跳过/完成状态。
- V3 不读写旧存档，完成档案重开保留，照片替换/删除/补录不改完成日期。
- 北京时间次日、晚到、宿主可信解锁、离线缓存；正式模式不信首次设备时钟。
- 宿主缺失/失败/超时/冲突，单路同步、草稿合并、幂等重试；账号切换后的旧回执隔离。
- 三种正午预测、水钟完成前拖动限制、静态降级、后台暂停、完成恢复不重复事件。
- 音频默认关闭、暂停/重播/重试、多段串行、换页旧回调失效、实体翻面后解锁。
- 真实上传前置、无回执不假成功、接力空/失败/拒绝/撤回、私人稿与公开副本分离。
- 报告多页导出、完整长笔记、保存失败重试、照片与示意图分离。
- 页面/组件/JS引用、WXML动态表达式、62个资源哈希、有效录音不被发布忽略。

## UI 与 Canvas

12 个页面状态、375px 与 1100px 两种宽度，共 34 张浏览器台架截图：无脚本错误、图片加载失败或横向溢出。包含 H4 到达前/后、HY1、DS1/DS2、FN4、LT6/LT7。报告 Canvas 和水钟 Canvas 另做真实 2D 绘图检查。

这些截图是台架验证，不能替代微信原生控件、音频和权限测试。宽屏会按 rpx 放大，当前主要针对手机。

## 微信开发者工具

本机已装 Stable v2.02.2608070（arm64）。通过工具 UI 导入本仓库，首页进入 walk 成功，本地模拟器错误数为 0。工具实际选择基础库 3.17.3（灰度）；项目声明 2.30.0。CLI 自动化因本机服务端口关闭未启用，没有调整安全设置。

水钟组件 CSS 已改为 class 选择器。海晏堂固定画布问题已在原生开发者工具复现并修复，详细见 `v3-water-clock-fix.md`；最新自动化回归为 109/109。

## 包体积

按 `project.config.json` 排除清单计算未压缩真实文件字节：主包约 0.71 MiB，游戏分包约 0.87 MiB，最大音频分包约 1.84 MiB，总计约 5.93 MiB。原型、文档、测试、许可证源文件及冻结录音均排除发布。

这是源文件字节统计，不声称微信最终上传压缩包大小；最终发布须再执行开发者工具代码包分析。当前工程预算为主包/每分包 2 MiB、总计 20 MiB。

## 尚需外部条件的验收

- iOS、Android 真机：Canvas 性能、静态降级、相册/相机权限、NFC、音频、杀进程与后台恢复。
- 37 个新版录音逐条听审及16个冻结条目的确认/补录，见音频清单。
- 官方宿主：真实身份、存档冲突策略、上传域名、审核/撤回服务、可信来信和订阅消息联调。
- 上线前以园区现场标识及开放情况核对路线。当前没有启用未经实测的经纬度导航。
