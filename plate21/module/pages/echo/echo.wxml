<nav-back />
<view class="fx-page page-shell paper-grain"><view class="fx-kicker">AFTER THE VISIT · 次日</view><view class="fx-title">离园之后，再翻一页</view><view wx:if="{{!completed}}" class="fx-card"><view class="fx-note">结束今日体验后，这里会记录下一次开放时间。无需答对全部题目，也无需上传照片。</view><button bindtap="journal">前往私人记录</button></view><view wx:elif="{{!unlocked}}" class="fx-card"><view class="fx-card-title">{{unlockLabel}}</view><view class="fx-note">届时可查看已发布内容，之后持续开放，不必准时上线。</view></view><block wx:else><view wx:if="{{letter}}" class="fx-body">{{letter.text}}</view><view wx:if="{{!chapters.length}}" class="fx-card"><view class="fx-card-title">暂时没有已发布的附页</view><view class="fx-note">今日记录已经保存。新的内容发布后，会显示在这里。</view></view><view wx:for="{{chapters}}" wx:key="id" class="fx-card"><view class="fx-card-title">{{item.title}}</view><view class="fx-note">{{item.summary}}</view><button bindtap="open" data-id="{{item.id}}">{{item.progress.completed ? '重新阅读' : '继续阅读'}}</button></view></block></view>

<page-overlays />
