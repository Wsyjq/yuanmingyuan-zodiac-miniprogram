<view
  wx:if="{{visible}}"
  class="stamp-toast {{fading ? 'fading' : ''}}"
  style="--rot: {{rotate}}deg; transform: translateY({{ty}}rpx) scale({{scale}}); opacity: {{opacity}}"
>
  <view class="seal serif">{{text}}</view>
</view>
